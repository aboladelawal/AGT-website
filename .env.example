# Copy this file to .env and fill in real values. Never commit .env (it is gitignored).

# Resend API key — create one at https://resend.com/api-keys
# Used by src/pages/api/contact.ts to send contact-form enquiries.
RESEND_API_KEY=
